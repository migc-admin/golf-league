// Temporary local-only preview route — NOT linked anywhere in the app.
// Used to review the new hero design before swapping it into Home.jsx.
import HeroTwoButtons from '../../components/ui/HeroTwoButtons'

export default function HeroPreview() {
  return <HeroTwoButtons />
}
